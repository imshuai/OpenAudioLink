# Phase 0 Bootstrap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the documentation-only repository into a buildable Phase 0 skeleton with shared protocol golden packets, minimal Android and Windows projects, and CI gates.

**Architecture:** Keep Phase 0 small: Python stdlib scripts generate and verify protocol golden packets, while Android and Windows each implement only the common packet header and AUDIO payload validation. UI, audio devices, discovery, installers, Hilt, Room, NAudio, mDNS, and configuration persistence are deferred until the skeleton builds and protocol tests pass.

**Tech Stack:** Python 3 stdlib, C# targeting .NET Framework 4.8 with WinForms and MSTest, Android Gradle Plugin 8.5.2, Gradle 8.7, Kotlin 2.0.21, Java 17, GitHub Actions.

---

## Scope Check

This plan implements `docs/11-Roadmap.md` Phase 0 only.

Included:

- Repository scaffolding.
- Protocol string length rule clarification.
- Protocol golden packet generation.
- Minimal Windows Receiver skeleton.
- Minimal Android Sender skeleton.
- Minimal CI.

Excluded from Phase 0:

- Real audio capture.
- Real AAC encoding or decoding.
- TCP streaming.
- mDNS discovery.
- Windows installer.
- Android foreground service.
- Full UI.

Those belong to Phase 1 plans after Phase 0 is green.

---

## File Structure

Create this structure:

```text
.github/
└── workflows/
    ├── docs.yml
    ├── windows.yml
    └── android.yml

tools/
├── check_docs_consistency.py
└── protocol/
    └── generate_golden_packets.py

testdata/
├── README.md
└── protocol/
    ├── golden-manifest.json
    ├── invalid-length.bin
    ├── invalid-magic.bin
    ├── valid-audio-aac.bin
    └── valid-hello.bin

receiver-windows/
├── OpenAudioLink.sln
├── src/
│   └── OpenAudioLink/
│       ├── OpenAudioLink.csproj
│       ├── Program.cs
│       ├── UI/
│       │   └── MainForm.cs
│       └── Protocol/
│           ├── AudioPayloadValidator.cs
│           ├── PacketHeader.cs
│           ├── PacketParseException.cs
│           ├── PacketParser.cs
│           └── ProtocolConstants.cs
└── tests/
    └── OpenAudioLink.Tests/
        ├── OpenAudioLink.Tests.csproj
        └── Protocol/
            └── PacketParserTests.cs

sender-android/
├── build.gradle.kts
├── settings.gradle.kts
├── gradle.properties
├── gradlew
├── gradlew.bat
├── gradle/
│   └── wrapper/
│       ├── gradle-wrapper.jar
│       └── gradle-wrapper.properties
└── app/
    ├── build.gradle.kts
    └── src/
        ├── main/
        │   ├── AndroidManifest.xml
        │   └── java/com/openaudiolink/
        │       ├── MainActivity.kt
        │       └── protocol/
        │           ├── AudioPayloadValidator.kt
        │           ├── PacketHeader.kt
        │           ├── PacketParseException.kt
        │           ├── PacketParser.kt
        │           └── ProtocolConstants.kt
        └── test/java/com/openaudiolink/protocol/
            └── PacketParserTest.kt
```

---

### Task 1: Add repository scaffolding and docs consistency check

**Files:**

- Create: `.gitignore`
- Create: `testdata/README.md`
- Create: `tools/check_docs_consistency.py`

- [ ] **Step 1: Create repository support directories**

Run:

```bash
mkdir -p tools/protocol testdata/protocol .github/workflows
```

Expected: command exits 0.

- [ ] **Step 2: Create `.gitignore`**

Write `./.gitignore`:

```gitignore
# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc

# Windows / .NET
receiver-windows/**/bin/
receiver-windows/**/obj/
receiver-windows/.vs/
*.user
*.suo

# Android / Gradle
sender-android/.gradle/
sender-android/local.properties
sender-android/**/build/

# Logs
*.log
```

- [ ] **Step 3: Create `testdata/README.md`**

Write `./testdata/README.md`:

```markdown
# Test Data

This directory stores deterministic test fixtures shared by the Android and Windows implementations.

`testdata/protocol/` contains binary golden packets generated by `tools/protocol/generate_golden_packets.py`.

Golden packet files must be regenerated only when `docs/03-Protocol.md` changes intentionally.
```

- [ ] **Step 4: Create docs consistency script**

Write `./tools/check_docs_consistency.py`:

```python
#!/usr/bin/env python3
from __future__ import annotations

import re
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
DOCS = sorted((ROOT / "docs").glob("*.md"))
FILES = [ROOT / "README.md", *DOCS]
REQUIRED_DOCS = [
    "docs/01-Introduction.md",
    "docs/02-Architecture.md",
    "docs/03-Protocol.md",
    "docs/04-Android.md",
    "docs/05-Windows.md",
    "docs/06-Audio.md",
    "docs/07-Discovery.md",
    "docs/08-Configuration.md",
    "docs/09-Deployment.md",
    "docs/10-Testing.md",
    "docs/11-Roadmap.md",
]
STALE_PATTERNS = [
    "DLNA Player Roadmap",
    "Android 8.0+",
    ".NET 8",
    "Self-contained",
    "self-contained",
    "dotnet publish",
    "Host.CreateDefaultBuilder",
    "Generic Host",
    "12-FAQ.md",
    "Encoded Size matches Payload Length",
]


def read(path: Path) -> str:
    return path.read_text(encoding="utf-8")


def check_required_docs() -> list[str]:
    errors: list[str] = []
    for rel in REQUIRED_DOCS:
        if not (ROOT / rel).exists():
            errors.append(f"missing required document: {rel}")
    return errors


def check_markdown_fences() -> list[str]:
    errors: list[str] = []
    for path in FILES:
        if read(path).count("```") % 2:
            errors.append(f"unbalanced markdown fence: {path.relative_to(ROOT)}")
    return errors


def check_doc_refs() -> list[str]:
    errors: list[str] = []
    pattern = re.compile(r"docs/[0-9]{2}-[A-Za-z]+\.md")
    for path in FILES:
        for ref in pattern.findall(read(path)):
            if not (ROOT / ref).exists():
                errors.append(f"missing doc ref in {path.relative_to(ROOT)}: {ref}")
    return errors


def check_stale_text() -> list[str]:
    errors: list[str] = []
    for path in FILES:
        text = read(path)
        for pattern in STALE_PATTERNS:
            if pattern in text:
                errors.append(f"stale text in {path.relative_to(ROOT)}: {pattern}")
    return errors


def main() -> int:
    errors = []
    errors.extend(check_required_docs())
    errors.extend(check_markdown_fences())
    errors.extend(check_doc_refs())
    errors.extend(check_stale_text())
    if errors:
        for error in errors:
            print(error)
        return 1
    print(f"docs consistency ok: {len(FILES)} markdown files checked")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 5: Run docs consistency check**

Run:

```bash
python3 tools/check_docs_consistency.py
```

Expected output:

```text
docs consistency ok: 12 markdown files checked
```

- [ ] **Step 6: Commit Task 1**

Run:

```bash
git add .gitignore testdata/README.md tools/check_docs_consistency.py
git commit -m "chore: add repository consistency checks"
```

Expected: commit succeeds.

---

### Task 2: Clarify protocol string encoding

**Files:**

- Modify: `docs/03-Protocol.md`

- [ ] **Step 1: Insert exact string layout under Character Encoding**

In `docs/03-Protocol.md`, under `# Character Encoding`, replace the short text after `Every string is length-prefixed.` with this full block:

````markdown
Every string is length-prefixed.

String layout:

| Offset | Size | Field |
|--------:|-----:|------|
| 0 | 2 | Byte Length |
| 2 | N | UTF-8 Bytes |

`Byte Length` is an unsigned 16-bit integer using Big Endian byte order.

The length counts UTF-8 bytes only.

It does not include the two-byte length field.

Null termination is prohibited.

Recommended maximum string length:

```text
512 bytes
```

A receiver must reject strings whose declared length exceeds the remaining payload.
````

- [ ] **Step 2: Run docs consistency check**

Run:

```bash
python3 tools/check_docs_consistency.py
```

Expected output:

```text
docs consistency ok: 12 markdown files checked
```

- [ ] **Step 3: Commit Task 2**

Run:

```bash
git add docs/03-Protocol.md
git commit -m "docs: define protocol string layout"
```

Expected: commit succeeds.

---

### Task 3: Generate shared protocol golden packets

**Files:**

- Create: `tools/protocol/generate_golden_packets.py`
- Generate: `testdata/protocol/golden-manifest.json`
- Generate: `testdata/protocol/valid-hello.bin`
- Generate: `testdata/protocol/valid-audio-aac.bin`
- Generate: `testdata/protocol/invalid-magic.bin`
- Generate: `testdata/protocol/invalid-length.bin`

- [ ] **Step 1: Create golden packet generator**

Write `./tools/protocol/generate_golden_packets.py`:

```python
#!/usr/bin/env python3
from __future__ import annotations

import argparse
import hashlib
import json
import struct
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
OUT = ROOT / "testdata" / "protocol"

MAGIC = b"OALP"
MAJOR = 1
MINOR = 0
HEADER_SIZE = 24
MAX_PACKET_SIZE = 65536
PACKET_HELLO = 0x01
PACKET_AUDIO = 0x05
CODEC_AAC_LC = 1
PLATFORM_ANDROID = 1
CAP_AAC_SUPPORTED = 1


def pack_header(packet_type: int, sequence: int, timestamp: int, payload: bytes) -> bytes:
    return b"".join(
        [
            MAGIC,
            struct.pack(">B", MAJOR),
            struct.pack(">B", MINOR),
            struct.pack(">B", packet_type),
            struct.pack(">B", 0),
            struct.pack(">I", sequence),
            struct.pack(">Q", timestamp),
            struct.pack(">I", len(payload)),
            payload,
        ]
    )


def pack_string(value: str) -> bytes:
    data = value.encode("utf-8")
    if len(data) > 512:
        raise ValueError("protocol string exceeds 512 bytes")
    return struct.pack(">H", len(data)) + data


def hello_payload() -> bytes:
    return b"".join(
        [
            pack_string("Android Phone"),
            pack_string("1.0.0"),
            struct.pack(">B", MAJOR),
            struct.pack(">B", MINOR),
            struct.pack(">B", PLATFORM_ANDROID),
            struct.pack(">I", CAP_AAC_SUPPORTED),
        ]
    )


def audio_payload() -> bytes:
    encoded = bytes([0x11, 0x22, 0x33, 0x44])
    return b"".join(
        [
            struct.pack(">B", CODEC_AAC_LC),
            struct.pack(">I", 1),
            struct.pack(">Q", 123456789),
            struct.pack(">H", 20),
            struct.pack(">I", len(encoded)),
            encoded,
        ]
    )


def packet_set() -> dict[str, bytes]:
    valid_hello = pack_header(PACKET_HELLO, 1, 123456000, hello_payload())
    valid_audio = pack_header(PACKET_AUDIO, 2, 123456789, audio_payload())
    invalid_magic = b"BAD!" + valid_hello[4:]
    invalid_length = b"".join(
        [
            MAGIC,
            struct.pack(">B", MAJOR),
            struct.pack(">B", MINOR),
            struct.pack(">B", PACKET_AUDIO),
            struct.pack(">B", 0),
            struct.pack(">I", 3),
            struct.pack(">Q", 123456790),
            struct.pack(">I", MAX_PACKET_SIZE + 1),
        ]
    )
    return {
        "valid-hello.bin": valid_hello,
        "valid-audio-aac.bin": valid_audio,
        "invalid-magic.bin": invalid_magic,
        "invalid-length.bin": invalid_length,
    }


def manifest(files: dict[str, bytes]) -> dict[str, object]:
    return {
        "protocol": "1.0",
        "headerSize": HEADER_SIZE,
        "maxPacketSize": MAX_PACKET_SIZE,
        "byteOrder": "big-endian",
        "files": {
            name: {"length": len(data), "sha256": hashlib.sha256(data).hexdigest()}
            for name, data in sorted(files.items())
        },
    }


def write_files() -> None:
    OUT.mkdir(parents=True, exist_ok=True)
    files = packet_set()
    for name, data in files.items():
        (OUT / name).write_bytes(data)
    (OUT / "golden-manifest.json").write_text(
        json.dumps(manifest(files), indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )


def check_files() -> int:
    files = packet_set()
    expected_manifest = json.dumps(manifest(files), indent=2, sort_keys=True) + "\n"
    errors: list[str] = []
    for name, data in files.items():
        path = OUT / name
        if not path.exists():
            errors.append(f"missing {path}")
        elif path.read_bytes() != data:
            errors.append(f"changed {path}")
    manifest_path = OUT / "golden-manifest.json"
    if not manifest_path.exists():
        errors.append(f"missing {manifest_path}")
    elif manifest_path.read_text(encoding="utf-8") != expected_manifest:
        errors.append(f"changed {manifest_path}")
    if errors:
        for error in errors:
            print(error)
        return 1
    print("protocol golden packets ok")
    return 0


def main() -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--check", action="store_true")
    args = parser.parse_args()
    if args.check:
        return check_files()
    write_files()
    print(f"wrote protocol golden packets to {OUT.relative_to(ROOT)}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Generate golden packet files**

Run:

```bash
python3 tools/protocol/generate_golden_packets.py
```

Expected output:

```text
wrote protocol golden packets to testdata/protocol
```

- [ ] **Step 3: Verify generated files are stable**

Run:

```bash
python3 tools/protocol/generate_golden_packets.py --check
```

Expected output:

```text
protocol golden packets ok
```

- [ ] **Step 4: Inspect manifest**

Run:

```bash
python3 -m json.tool testdata/protocol/golden-manifest.json >/dev/null
```

Expected: command exits 0.

- [ ] **Step 5: Commit Task 3**

Run:

```bash
git add tools/protocol/generate_golden_packets.py testdata/protocol
git commit -m "test: add protocol golden packets"
```

Expected: commit succeeds.

---

### Task 4: Create Windows Receiver skeleton and protocol tests

**Files:**

- Create: `receiver-windows/OpenAudioLink.sln`
- Create: `receiver-windows/src/OpenAudioLink/OpenAudioLink.csproj`
- Create: `receiver-windows/src/OpenAudioLink/Program.cs`
- Create: `receiver-windows/src/OpenAudioLink/UI/MainForm.cs`
- Create: `receiver-windows/src/OpenAudioLink/Protocol/ProtocolConstants.cs`
- Create: `receiver-windows/src/OpenAudioLink/Protocol/PacketHeader.cs`
- Create: `receiver-windows/src/OpenAudioLink/Protocol/PacketParseException.cs`
- Create: `receiver-windows/src/OpenAudioLink/Protocol/PacketParser.cs`
- Create: `receiver-windows/src/OpenAudioLink/Protocol/AudioPayloadValidator.cs`
- Create: `receiver-windows/tests/OpenAudioLink.Tests/OpenAudioLink.Tests.csproj`
- Create: `receiver-windows/tests/OpenAudioLink.Tests/Protocol/PacketParserTests.cs`

- [ ] **Step 1: Create Windows project directories**

Run:

```bash
mkdir -p receiver-windows/src/OpenAudioLink/Protocol receiver-windows/src/OpenAudioLink/UI receiver-windows/tests/OpenAudioLink.Tests/Protocol
```

Expected: command exits 0.

- [ ] **Step 2: Create solution file on a Windows machine with Visual Studio Build Tools**

Run:

```powershell
cd receiver-windows
dotnet new sln -n OpenAudioLink
```

Expected output includes:

```text
The template "Solution File" was created successfully.
```

- [ ] **Step 3: Create Windows app project**

Write `receiver-windows/src/OpenAudioLink/OpenAudioLink.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <RootNamespace>OpenAudioLink</RootNamespace>
    <AssemblyName>OpenAudioLink</AssemblyName>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>
</Project>
```

- [ ] **Step 4: Create `Program.cs`**

Write `receiver-windows/src/OpenAudioLink/Program.cs`:

```csharp
using System;
using System.Windows.Forms;
using OpenAudioLink.UI;

namespace OpenAudioLink
{
    internal static class Program
    {
        [STAThread]
        private static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            Application.Run(new MainForm());
        }
    }
}
```

- [ ] **Step 5: Create minimal WinForms UI**

Write `receiver-windows/src/OpenAudioLink/UI/MainForm.cs`:

```csharp
using System.Drawing;
using System.Windows.Forms;

namespace OpenAudioLink.UI
{
    public sealed class MainForm : Form
    {
        public MainForm()
        {
            Text = "OpenAudioLink Receiver";
            Width = 480;
            Height = 240;
            StartPosition = FormStartPosition.CenterScreen;

            var label = new Label
            {
                AutoSize = true,
                Text = "OpenAudioLink Receiver Skeleton",
                Location = new Point(24, 24)
            };

            Controls.Add(label);
        }
    }
}
```

- [ ] **Step 6: Create protocol constants**

Write `receiver-windows/src/OpenAudioLink/Protocol/ProtocolConstants.cs`:

```csharp
namespace OpenAudioLink.Protocol
{
    public static class ProtocolConstants
    {
        public const int HeaderSize = 24;
        public const int AudioPayloadHeaderSize = 19;
        public const int MaxPacketSize = 65536;
        public const byte MajorVersion = 1;
        public const byte MinorVersion = 0;
        public const byte PacketTypeHello = 0x01;
        public const byte PacketTypeAudio = 0x05;
        public const byte CodecAacLc = 1;
        public static readonly byte[] Magic = { 0x4F, 0x41, 0x4C, 0x50 };
    }
}
```

- [ ] **Step 7: Create packet model and exception**

Write `receiver-windows/src/OpenAudioLink/Protocol/PacketHeader.cs`:

```csharp
namespace OpenAudioLink.Protocol
{
    public sealed class PacketHeader
    {
        public PacketHeader(byte majorVersion, byte minorVersion, byte packetType, byte flags, uint sequenceNumber, ulong timestamp, uint payloadLength)
        {
            MajorVersion = majorVersion;
            MinorVersion = minorVersion;
            PacketType = packetType;
            Flags = flags;
            SequenceNumber = sequenceNumber;
            Timestamp = timestamp;
            PayloadLength = payloadLength;
        }

        public byte MajorVersion { get; }
        public byte MinorVersion { get; }
        public byte PacketType { get; }
        public byte Flags { get; }
        public uint SequenceNumber { get; }
        public ulong Timestamp { get; }
        public uint PayloadLength { get; }
    }
}
```

Write `receiver-windows/src/OpenAudioLink/Protocol/PacketParseException.cs`:

```csharp
using System;

namespace OpenAudioLink.Protocol
{
    public sealed class PacketParseException : Exception
    {
        public PacketParseException(string message) : base(message)
        {
        }
    }
}
```

- [ ] **Step 8: Create packet parser**

Write `receiver-windows/src/OpenAudioLink/Protocol/PacketParser.cs`:

```csharp
using System;

namespace OpenAudioLink.Protocol
{
    public static class PacketParser
    {
        public static PacketHeader ParseHeader(byte[] packet)
        {
            if (packet == null)
            {
                throw new PacketParseException("packet is null");
            }

            if (packet.Length < ProtocolConstants.HeaderSize)
            {
                throw new PacketParseException("packet is shorter than header");
            }

            for (var i = 0; i < ProtocolConstants.Magic.Length; i++)
            {
                if (packet[i] != ProtocolConstants.Magic[i])
                {
                    throw new PacketParseException("invalid magic");
                }
            }

            var major = packet[4];
            if (major != ProtocolConstants.MajorVersion)
            {
                throw new PacketParseException("unsupported protocol major version");
            }

            var payloadLength = ReadUInt32(packet, 20);
            if (payloadLength > ProtocolConstants.MaxPacketSize)
            {
                throw new PacketParseException("payload length exceeds maximum packet size");
            }

            var requiredLength = ProtocolConstants.HeaderSize + checked((int)payloadLength);
            if (packet.Length < requiredLength)
            {
                throw new PacketParseException("packet is shorter than declared payload length");
            }

            return new PacketHeader(
                major,
                packet[5],
                packet[6],
                packet[7],
                ReadUInt32(packet, 8),
                ReadUInt64(packet, 12),
                payloadLength);
        }

        public static byte[] Payload(byte[] packet)
        {
            var header = ParseHeader(packet);
            var payload = new byte[header.PayloadLength];
            Buffer.BlockCopy(packet, ProtocolConstants.HeaderSize, payload, 0, payload.Length);
            return payload;
        }

        internal static uint ReadUInt32(byte[] data, int offset)
        {
            return ((uint)data[offset] << 24) |
                   ((uint)data[offset + 1] << 16) |
                   ((uint)data[offset + 2] << 8) |
                   data[offset + 3];
        }

        private static ulong ReadUInt64(byte[] data, int offset)
        {
            return ((ulong)data[offset] << 56) |
                   ((ulong)data[offset + 1] << 48) |
                   ((ulong)data[offset + 2] << 40) |
                   ((ulong)data[offset + 3] << 32) |
                   ((ulong)data[offset + 4] << 24) |
                   ((ulong)data[offset + 5] << 16) |
                   ((ulong)data[offset + 6] << 8) |
                   data[offset + 7];
        }
    }
}
```

- [ ] **Step 9: Create AUDIO payload validator**

Write `receiver-windows/src/OpenAudioLink/Protocol/AudioPayloadValidator.cs`:

```csharp
namespace OpenAudioLink.Protocol
{
    public static class AudioPayloadValidator
    {
        public static void ValidateAacPayload(byte[] payload)
        {
            if (payload == null)
            {
                throw new PacketParseException("audio payload is null");
            }

            if (payload.Length < ProtocolConstants.AudioPayloadHeaderSize)
            {
                throw new PacketParseException("audio payload is shorter than audio header");
            }

            if (payload[0] != ProtocolConstants.CodecAacLc)
            {
                throw new PacketParseException("unsupported audio codec");
            }

            var encodedSize = PacketParser.ReadUInt32(payload, 15);
            if (payload.Length != ProtocolConstants.AudioPayloadHeaderSize + encodedSize)
            {
                throw new PacketParseException("audio payload length does not match encoded size");
            }
        }
    }
}
```

- [ ] **Step 10: Create Windows test project**

Write `receiver-windows/tests/OpenAudioLink.Tests/OpenAudioLink.Tests.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net48</TargetFramework>
    <IsPackable>false</IsPackable>
    <RootNamespace>OpenAudioLink.Tests</RootNamespace>
    <AssemblyName>OpenAudioLink.Tests</AssemblyName>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="MSTest.TestAdapter" Version="3.5.2" />
    <PackageReference Include="MSTest.TestFramework" Version="3.5.2" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\OpenAudioLink\OpenAudioLink.csproj" />
  </ItemGroup>
</Project>
```

- [ ] **Step 11: Create Windows protocol tests**

Write `receiver-windows/tests/OpenAudioLink.Tests/Protocol/PacketParserTests.cs`:

```csharp
using System;
using System.IO;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using OpenAudioLink.Protocol;

namespace OpenAudioLink.Tests.Protocol
{
    [TestClass]
    public sealed class PacketParserTests
    {
        [TestMethod]
        public void ValidHelloHeaderParses()
        {
            var packet = ReadProtocolFixture("valid-hello.bin");
            var header = PacketParser.ParseHeader(packet);
            Assert.AreEqual(ProtocolConstants.MajorVersion, header.MajorVersion);
            Assert.AreEqual(ProtocolConstants.MinorVersion, header.MinorVersion);
            Assert.AreEqual(ProtocolConstants.PacketTypeHello, header.PacketType);
            Assert.AreEqual((uint)(packet.Length - ProtocolConstants.HeaderSize), header.PayloadLength);
        }

        [TestMethod]
        public void InvalidMagicIsRejected()
        {
            var packet = ReadProtocolFixture("invalid-magic.bin");
            Assert.ThrowsException<PacketParseException>(() => PacketParser.ParseHeader(packet));
        }

        [TestMethod]
        public void InvalidLengthIsRejected()
        {
            var packet = ReadProtocolFixture("invalid-length.bin");
            Assert.ThrowsException<PacketParseException>(() => PacketParser.ParseHeader(packet));
        }

        [TestMethod]
        public void ValidAacAudioPayloadPassesValidation()
        {
            var packet = ReadProtocolFixture("valid-audio-aac.bin");
            var header = PacketParser.ParseHeader(packet);
            Assert.AreEqual(ProtocolConstants.PacketTypeAudio, header.PacketType);
            AudioPayloadValidator.ValidateAacPayload(PacketParser.Payload(packet));
        }

        private static byte[] ReadProtocolFixture(string name)
        {
            var directory = new DirectoryInfo(AppDomain.CurrentDomain.BaseDirectory);
            while (directory != null)
            {
                var path = Path.Combine(directory.FullName, "testdata", "protocol", name);
                if (File.Exists(path))
                {
                    return File.ReadAllBytes(path);
                }
                directory = directory.Parent;
            }
            throw new FileNotFoundException("protocol fixture not found", name);
        }
    }
}
```

- [ ] **Step 12: Add projects to solution**

Run on Windows:

```powershell
cd receiver-windows
dotnet sln OpenAudioLink.sln add src/OpenAudioLink/OpenAudioLink.csproj
dotnet sln OpenAudioLink.sln add tests/OpenAudioLink.Tests/OpenAudioLink.Tests.csproj
```

Expected output includes both projects added.

- [ ] **Step 13: Run Windows tests**

Run on Windows:

```powershell
dotnet test receiver-windows/OpenAudioLink.sln -c Release
```

Expected output includes:

```text
Passed!  - Failed:     0
```

- [ ] **Step 14: Commit Task 4**

Run:

```bash
git add receiver-windows
git commit -m "chore: add Windows receiver skeleton"
```

Expected: commit succeeds.

---

### Task 5: Create Android Sender skeleton and protocol tests

**Files:**

- Create: `sender-android/settings.gradle.kts`
- Create: `sender-android/build.gradle.kts`
- Create: `sender-android/gradle.properties`
- Generate: `sender-android/gradlew`
- Generate: `sender-android/gradlew.bat`
- Generate: `sender-android/gradle/wrapper/gradle-wrapper.jar`
- Generate: `sender-android/gradle/wrapper/gradle-wrapper.properties`
- Create: `sender-android/app/build.gradle.kts`
- Create: `sender-android/app/src/main/AndroidManifest.xml`
- Create: `sender-android/app/src/main/java/com/openaudiolink/MainActivity.kt`
- Create: protocol Kotlin files under `sender-android/app/src/main/java/com/openaudiolink/protocol/`
- Create: `sender-android/app/src/test/java/com/openaudiolink/protocol/PacketParserTest.kt`

- [ ] **Step 1: Create Android directories**

Run:

```bash
mkdir -p sender-android/app/src/main/java/com/openaudiolink/protocol sender-android/app/src/test/java/com/openaudiolink/protocol
```

Expected: command exits 0.

- [ ] **Step 2: Create Gradle settings**

Write `sender-android/settings.gradle.kts`:

```kotlin
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "OpenAudioLinkSender"
include(":app")
```

- [ ] **Step 3: Create root Gradle build**

Write `sender-android/build.gradle.kts`:

```kotlin
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
```

- [ ] **Step 4: Create Gradle properties**

Write `sender-android/gradle.properties`:

```properties
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
```

- [ ] **Step 5: Create app Gradle build**

Write `sender-android/app/build.gradle.kts`:

```kotlin
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.openaudiolink"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.openaudiolink"
        minSdk = 29
        targetSdk = 35
        versionCode = 1
        versionName = "1.0.0"
        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }
}

dependencies {
    testImplementation("junit:junit:4.13.2")
}
```

- [ ] **Step 6: Generate Gradle wrapper**

Run from `sender-android/` on a machine with Java 17 and Gradle installed:

```bash
gradle wrapper --gradle-version 8.7
```

Expected output includes:

```text
BUILD SUCCESSFUL
```

- [ ] **Step 7: Create Android manifest**

Write `sender-android/app/src/main/AndroidManifest.xml`:

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:label="OpenAudioLink"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

- [ ] **Step 8: Create minimal Android activity**

Write `sender-android/app/src/main/java/com/openaudiolink/MainActivity.kt`:

```kotlin
package com.openaudiolink

import android.app.Activity
import android.os.Bundle
import android.widget.TextView

class MainActivity : Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(TextView(this).apply { text = "OpenAudioLink Sender Skeleton" })
    }
}
```

- [ ] **Step 9: Create Kotlin protocol constants and model**

Write `sender-android/app/src/main/java/com/openaudiolink/protocol/ProtocolConstants.kt`:

```kotlin
package com.openaudiolink.protocol

object ProtocolConstants {
    const val HEADER_SIZE = 24
    const val AUDIO_PAYLOAD_HEADER_SIZE = 19
    const val MAX_PACKET_SIZE = 65536
    const val MAJOR_VERSION = 1
    const val MINOR_VERSION = 0
    const val PACKET_TYPE_HELLO = 0x01
    const val PACKET_TYPE_AUDIO = 0x05
    const val CODEC_AAC_LC = 1
    val MAGIC = byteArrayOf(0x4F, 0x41, 0x4C, 0x50)
}
```

Write `sender-android/app/src/main/java/com/openaudiolink/protocol/PacketHeader.kt`:

```kotlin
package com.openaudiolink.protocol

data class PacketHeader(
    val majorVersion: Int,
    val minorVersion: Int,
    val packetType: Int,
    val flags: Int,
    val sequenceNumber: Long,
    val timestamp: Long,
    val payloadLength: Int,
)
```

Write `sender-android/app/src/main/java/com/openaudiolink/protocol/PacketParseException.kt`:

```kotlin
package com.openaudiolink.protocol

class PacketParseException(message: String) : IllegalArgumentException(message)
```

- [ ] **Step 10: Create Kotlin packet parser**

Write `sender-android/app/src/main/java/com/openaudiolink/protocol/PacketParser.kt`:

```kotlin
package com.openaudiolink.protocol

object PacketParser {
    fun parseHeader(packet: ByteArray): PacketHeader {
        if (packet.size < ProtocolConstants.HEADER_SIZE) {
            throw PacketParseException("packet is shorter than header")
        }

        ProtocolConstants.MAGIC.forEachIndexed { index, value ->
            if (packet[index] != value) {
                throw PacketParseException("invalid magic")
            }
        }

        val major = u8(packet[4])
        if (major != ProtocolConstants.MAJOR_VERSION) {
            throw PacketParseException("unsupported protocol major version")
        }

        val payloadLength = u32(packet, 20)
        if (payloadLength > ProtocolConstants.MAX_PACKET_SIZE) {
            throw PacketParseException("payload length exceeds maximum packet size")
        }

        val requiredLength = ProtocolConstants.HEADER_SIZE + payloadLength
        if (packet.size < requiredLength) {
            throw PacketParseException("packet is shorter than declared payload length")
        }

        return PacketHeader(
            majorVersion = major,
            minorVersion = u8(packet[5]),
            packetType = u8(packet[6]),
            flags = u8(packet[7]),
            sequenceNumber = u32(packet, 8).toLong(),
            timestamp = u64(packet, 12),
            payloadLength = payloadLength,
        )
    }

    fun payload(packet: ByteArray): ByteArray {
        val header = parseHeader(packet)
        return packet.copyOfRange(
            ProtocolConstants.HEADER_SIZE,
            ProtocolConstants.HEADER_SIZE + header.payloadLength,
        )
    }

    fun u32(data: ByteArray, offset: Int): Int {
        return (u8(data[offset]) shl 24) or
            (u8(data[offset + 1]) shl 16) or
            (u8(data[offset + 2]) shl 8) or
            u8(data[offset + 3])
    }

    private fun u64(data: ByteArray, offset: Int): Long {
        var value = 0L
        for (i in 0 until 8) {
            value = (value shl 8) or u8(data[offset + i]).toLong()
        }
        return value
    }

    private fun u8(value: Byte): Int = value.toInt() and 0xFF
}
```

- [ ] **Step 11: Create Kotlin AUDIO payload validator**

Write `sender-android/app/src/main/java/com/openaudiolink/protocol/AudioPayloadValidator.kt`:

```kotlin
package com.openaudiolink.protocol

object AudioPayloadValidator {
    fun validateAacPayload(payload: ByteArray) {
        if (payload.size < ProtocolConstants.AUDIO_PAYLOAD_HEADER_SIZE) {
            throw PacketParseException("audio payload is shorter than audio header")
        }

        val codec = payload[0].toInt() and 0xFF
        if (codec != ProtocolConstants.CODEC_AAC_LC) {
            throw PacketParseException("unsupported audio codec")
        }

        val encodedSize = PacketParser.u32(payload, 15)
        if (payload.size != ProtocolConstants.AUDIO_PAYLOAD_HEADER_SIZE + encodedSize) {
            throw PacketParseException("audio payload length does not match encoded size")
        }
    }
}
```

- [ ] **Step 12: Create Android protocol tests**

Write `sender-android/app/src/test/java/com/openaudiolink/protocol/PacketParserTest.kt`:

```kotlin
package com.openaudiolink.protocol

import org.junit.Assert.assertEquals
import org.junit.Test
import java.io.File

class PacketParserTest {
    @Test
    fun validHelloHeaderParses() {
        val packet = fixture("valid-hello.bin")
        val header = PacketParser.parseHeader(packet)
        assertEquals(ProtocolConstants.MAJOR_VERSION, header.majorVersion)
        assertEquals(ProtocolConstants.MINOR_VERSION, header.minorVersion)
        assertEquals(ProtocolConstants.PACKET_TYPE_HELLO, header.packetType)
        assertEquals(packet.size - ProtocolConstants.HEADER_SIZE, header.payloadLength)
    }

    @Test(expected = PacketParseException::class)
    fun invalidMagicIsRejected() {
        PacketParser.parseHeader(fixture("invalid-magic.bin"))
    }

    @Test(expected = PacketParseException::class)
    fun invalidLengthIsRejected() {
        PacketParser.parseHeader(fixture("invalid-length.bin"))
    }

    @Test
    fun validAacAudioPayloadPassesValidation() {
        val packet = fixture("valid-audio-aac.bin")
        val header = PacketParser.parseHeader(packet)
        assertEquals(ProtocolConstants.PACKET_TYPE_AUDIO, header.packetType)
        AudioPayloadValidator.validateAacPayload(PacketParser.payload(packet))
    }

    private fun fixture(name: String): ByteArray {
        var directory = File(System.getProperty("user.dir")).absoluteFile
        while (true) {
            val file = File(directory, "../testdata/protocol/$name").canonicalFile
            if (file.exists()) return file.readBytes()
            val parent = directory.parentFile ?: break
            directory = parent
        }
        error("protocol fixture not found: $name")
    }
}
```

- [ ] **Step 13: Run Android unit tests**

Run:

```bash
cd sender-android
./gradlew :app:testDebugUnitTest
```

Expected output includes:

```text
BUILD SUCCESSFUL
```

- [ ] **Step 14: Commit Task 5**

Run:

```bash
git add sender-android
git commit -m "chore: add Android sender skeleton"
```

Expected: commit succeeds.

---

### Task 6: Add minimal CI workflows

**Files:**

- Create: `.github/workflows/docs.yml`
- Create: `.github/workflows/windows.yml`
- Create: `.github/workflows/android.yml`

- [ ] **Step 1: Create docs workflow**

Write `.github/workflows/docs.yml`:

```yaml
name: docs

on:
  pull_request:
  push:
    branches: [main]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python3 tools/check_docs_consistency.py
      - run: python3 tools/protocol/generate_golden_packets.py --check
```

- [ ] **Step 2: Create Windows workflow**

Write `.github/workflows/windows.yml`:

```yaml
name: windows

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: microsoft/setup-msbuild@v2
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - run: dotnet test receiver-windows/OpenAudioLink.sln -c Release
```

- [ ] **Step 3: Create Android workflow**

Write `.github/workflows/android.yml`:

```yaml
name: android

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - run: ./gradlew :app:testDebugUnitTest
        working-directory: sender-android
```

- [ ] **Step 4: Run local docs workflow commands**

Run:

```bash
python3 tools/check_docs_consistency.py
python3 tools/protocol/generate_golden_packets.py --check
```

Expected output includes:

```text
docs consistency ok: 12 markdown files checked
protocol golden packets ok
```

- [ ] **Step 5: Commit Task 6**

Run:

```bash
git add .github/workflows
git commit -m "ci: add Phase 0 validation workflows"
```

Expected: commit succeeds.

---

### Task 7: Final Phase 0 verification

**Files:**

- Read: `docs/10-Testing.md`
- Read: `docs/11-Roadmap.md`
- Read: all generated skeleton files

- [ ] **Step 1: Run docs checks**

Run:

```bash
python3 tools/check_docs_consistency.py
python3 tools/protocol/generate_golden_packets.py --check
```

Expected output includes:

```text
docs consistency ok: 12 markdown files checked
protocol golden packets ok
```

- [ ] **Step 2: Run Windows tests on Windows**

Run:

```powershell
dotnet test receiver-windows/OpenAudioLink.sln -c Release
```

Expected output includes:

```text
Passed!  - Failed:     0
```

- [ ] **Step 3: Run Android tests**

Run:

```bash
cd sender-android
./gradlew :app:testDebugUnitTest
```

Expected output includes:

```text
BUILD SUCCESSFUL
```

- [ ] **Step 4: Verify git state**

Run:

```bash
git status --short
```

Expected: no output.

- [ ] **Step 5: Push Phase 0 branch**

Run:

```bash
git push origin HEAD
```

Expected: branch is pushed.

---

## Self-Review

Spec coverage:

- `docs/11-Roadmap.md` Phase 0 project directories: Tasks 1, 4, 5.
- `docs/11-Roadmap.md` protocol golden packet tests: Tasks 2, 3, 4, 5.
- `docs/11-Roadmap.md` Android skeleton build: Task 5.
- `docs/11-Roadmap.md` Windows skeleton build: Task 4.
- `docs/11-Roadmap.md` basic CI: Task 6.
- `docs/10-Testing.md` static checks: Task 1 and Task 6.
- `docs/03-Protocol.md` packet layout: Tasks 2, 3, 4, 5.

Placeholder scan:

- No incomplete requirement markers are used.
- Every code-writing step includes concrete file content.
- Every verification step includes an exact command and expected result.

Type consistency:

- C# and Kotlin constants use the same values: header size 24, audio payload header size 19, max packet size 65536, magic `OALP`, protocol version 1.0, AAC-LC codec id 1.
- Golden packet generator uses the same constants.
- Tests in both platforms read the same `testdata/protocol/*.bin` fixtures.
